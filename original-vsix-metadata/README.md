# Original package metadata

These files came from the unpacked 1.0.0 VSIX. They are retained for reference only and are not build inputs. Always generate new VSIX metadata with `npm run package` inside `extension/`.
